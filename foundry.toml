[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200

# OpenZeppelin dependency (pinned target): v5.0.2
# Install command (for later implementation phase):
# forge install OpenZeppelin/openzeppelin-contracts@v5.0.2 --no-commit
